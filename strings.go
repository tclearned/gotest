package gotest

// RemoveSubstring should remove the given substring (sub) from the given string (str)
// e.g. given the inputs "abaaba", "aa" it should return "abba"
// hint: doing this is easy using the "strings" package, look up the godoc for "strings"
func RemoveSubstring(str, sub string) string {
	return str
}

